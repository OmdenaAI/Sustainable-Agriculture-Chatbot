# dictionary to map models to chunk size. Team C is expected to include as many models as they used
# alongside the chunk size they used for each model.
models_to_chunk_size_mapping = {
    "text-embedding-3-small": 40, #this is going to be 1024 after finishing development 
}