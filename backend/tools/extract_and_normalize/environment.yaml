name: extract-and-normalize
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - requests
  - urllib3  # for faster HTML parsing
  - pyyaml
  - nltk
  - pip:
      - openai
      - groq  # Added Groq client for alternative LLM provider
      - python-dotenv
      - PyMuPDF
      - jsonschema
      - tenacity
